services:
  chatbot_backend:
    build:
      args:
        BASE_IMAGE: nvidia/cuda:12.4.1-cudnn-devel-ubuntu22.04
        REQUIREMENTS_FILE: requirements.txt
        LLAMA_CUDA: "ON"
    environment:
      - USE_GPU=true
      - LLM_N_GPU_LAYERS=-1
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]